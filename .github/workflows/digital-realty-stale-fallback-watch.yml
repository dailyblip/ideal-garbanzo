name: Digital Realty stale fallback watchdog

on:
  schedule:
    - cron: '29 */6 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/collect-digital-realty.mjs'
      - 'scripts/enforce-digital-realty-fallback-freshness.mjs'
      - 'scripts/preserve-source-group.mjs'
      - 'scripts/validate-digital-realty.mjs'
      - '.github/workflows/digital-realty-stale-fallback-watch.yml'

permissions:
  actions: write
  contents: write

concurrency:
  group: digital-realty-stale-fallback-watch
  cancel-in-progress: false

jobs:
  enforce:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Test Digital Realty fallback age policy and source boundary
        run: |
          node scripts/enforce-digital-realty-fallback-freshness.mjs --test
          node scripts/preserve-source-group.mjs --test
      - name: Capture pre-enforcement public-feed state
        run: cp data/jobs.json /tmp/digital-realty-freshness-baseline.json
      - name: Enforce Digital Realty fallback freshness
        run: node scripts/enforce-digital-realty-fallback-freshness.mjs
      - name: Verify Digital Realty fallback enforcement stayed source-scoped
        run: node scripts/preserve-source-group.mjs "Digital Realty" /tmp/digital-realty-freshness-baseline.json --verify
      - name: Validate Digital Realty source state
        run: |
          node scripts/validate.mjs
          node scripts/validate-job-history.mjs --allow-aging
          node scripts/validate-digital-realty.mjs
          node scripts/validate-source-workflow-isolation.mjs
      - name: Publish Digital Realty freshness state from current main
        id: commit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          rebuild_from_latest_main() {
            git fetch origin main
            git reset --hard origin/main
            cp data/jobs.json /tmp/digital-realty-freshness-baseline.json
            node scripts/enforce-digital-realty-fallback-freshness.mjs --test
            node scripts/preserve-source-group.mjs --test
            node scripts/enforce-digital-realty-fallback-freshness.mjs
            node scripts/preserve-source-group.mjs "Digital Realty" /tmp/digital-realty-freshness-baseline.json --verify
            node scripts/validate.mjs
            node scripts/validate-job-history.mjs --allow-aging
            node scripts/validate-digital-realty.mjs
            node scripts/validate-source-workflow-isolation.mjs
          }

          git fetch origin main
          if [ "$(git rev-parse HEAD)" != "$(git rev-parse origin/main)" ]; then
            echo "Main moved during Digital Realty freshness enforcement; rebuilding from latest main."
            rebuild_from_latest_main
          fi

          for attempt in 1 2 3; do
            git add -u -- data/jobs.json data/digital-realty-jobs.json data/collector-status.json
            if git diff --cached --quiet; then
              echo "Digital Realty fallback state is unchanged."
              echo "changed=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi

            git commit -m "Enforce Digital Realty fallback freshness"
            if git push origin HEAD:main; then
              echo "changed=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi

            echo "Main moved during Digital Realty freshness publish attempt ${attempt}; rebuilding before retry."
            sleep $((attempt * 2))
            rebuild_from_latest_main
          done

          echo "::error::Could not publish Digital Realty freshness state after three attempts."
          exit 1
      - name: Deploy Digital Realty freshness change
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run pages.yml --repo "$GITHUB_REPOSITORY" --ref main -f deploy_only=true
