name: Refresh EdgeConneX roles

on:
  push:
    branches: [main]
    paths:
      - scripts/collect-edgeconnex-careers.mjs
      - scripts/validate-edgeconnex.mjs
      - .github/workflows/edgeconnex-bootstrap.yml
  workflow_dispatch:
  schedule:
    - cron: '23 */12 * * *'

permissions:
  contents: write
  actions: write

concurrency:
  group: careers-source-writers
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate EdgeConneX collector syntax
        run: |
          node --check scripts/collect-edgeconnex-careers.mjs
          node --check scripts/validate-edgeconnex.mjs
      - name: Collect official EdgeConneX Rippling roles
        run: node scripts/collect-edgeconnex-careers.mjs
      - name: Enforce mission-fit role families
        run: node scripts/filter-mission-fit.mjs
      - name: Normalize locations and regions
        run: node scripts/normalize-job-locations.mjs
      - name: Deduplicate normalized jobs
        run: node scripts/dedupe-normalized-jobs.mjs
      - name: Run live QA
        run: node scripts/qa-site.mjs
      - name: Refresh regional coverage
        run: node scripts/refresh-region-status.mjs
      - name: Validate EdgeConneX employer-direct snapshot
        run: node scripts/validate-edgeconnex.mjs
      - name: Track first-seen dates
        run: node scripts/stamp-job-history.mjs
      - name: Validate updated feed
        run: |
          node scripts/validate.mjs
          node scripts/validate-job-history.mjs
          node scripts/validate-homepage-contract.mjs
          node scripts/validate-source-workflow-isolation.mjs
      - name: Commit verified EdgeConneX roles
        id: commit
        run: |
          if [ -z "$(git status --porcelain -- data/jobs.json data/edgeconnex-jobs.json data/job-history.json data/collector-status.json data/career-events.json data/qa-report.json)" ]; then
            echo "No verified EdgeConneX data changes."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/jobs.json data/edgeconnex-jobs.json data/job-history.json data/collector-status.json data/career-events.json data/qa-report.json
          git commit -m "Refresh verified EdgeConneX roles"
          git fetch origin main
          if ! git rebase origin/main; then
            git rebase --abort || true
            echo "::error::Could not rebase EdgeConneX refresh onto latest main."
            exit 1
          fi
          git push origin HEAD:main
          echo "changed=true" >> "$GITHUB_OUTPUT"
      - name: Deploy committed EdgeConneX data
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run pages.yml --repo "$GITHUB_REPOSITORY" --ref main -f deploy_only=true
