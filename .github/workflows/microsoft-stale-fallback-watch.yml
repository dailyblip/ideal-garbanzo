name: Microsoft stale fallback watchdog

on:
  schedule:
    - cron: '19 */6 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/enforce-microsoft-fallback-freshness.mjs'
      - 'scripts/apply-microsoft-verified-fallback.mjs'
      - 'scripts/persist-microsoft-snapshot.mjs'
      - 'scripts/validate-microsoft-snapshot.mjs'
      - '.github/workflows/microsoft-stale-fallback-watch.yml'

permissions:
  actions: write
  contents: write

concurrency:
  group: microsoft-stale-fallback-watch
  cancel-in-progress: false

jobs:
  enforce:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Test Microsoft fallback age policy
        run: node scripts/enforce-microsoft-fallback-freshness.mjs --test
      - name: Enforce Microsoft fallback freshness
        run: node scripts/enforce-microsoft-fallback-freshness.mjs
      - name: Validate Microsoft snapshot state
        run: node scripts/validate-microsoft-snapshot.mjs
      - name: Publish Microsoft freshness state from current main
        id: commit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          rebuild_from_latest_main() {
            git fetch origin main
            git reset --hard origin/main
            node scripts/enforce-microsoft-fallback-freshness.mjs --test
            node scripts/enforce-microsoft-fallback-freshness.mjs
            node scripts/validate-microsoft-snapshot.mjs
          }

          git fetch origin main
          if [ "$(git rev-parse HEAD)" != "$(git rev-parse origin/main)" ]; then
            echo "Main moved during Microsoft freshness enforcement; rebuilding from latest main."
            rebuild_from_latest_main
          fi

          for attempt in 1 2 3; do
            git add -u -- data/jobs.json data/collector-status.json
            if git diff --cached --quiet; then
              echo "Microsoft fallback state is unchanged."
              echo "changed=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi

            git commit -m "Enforce Microsoft fallback freshness"
            if git push origin HEAD:main; then
              echo "changed=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi

            echo "Main moved during Microsoft freshness publish attempt ${attempt}; rebuilding before retry."
            sleep $((attempt * 2))
            rebuild_from_latest_main
          done

          echo "::error::Could not publish Microsoft freshness state after three attempts."
          exit 1
      - name: Deploy Microsoft freshness change
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run pages.yml --repo "$GITHUB_REPOSITORY" --ref main -f deploy_only=true
