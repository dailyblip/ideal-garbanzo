name: Refresh Novva Data Centers roles

on:
  push:
    branches: [main]
    paths:
      - scripts/collect-novva-careers.mjs
      - scripts/validate-novva.mjs
      - .github/workflows/novva-bootstrap.yml
  workflow_dispatch:
  schedule:
    - cron: '47 */12 * * *'

permissions:
  contents: write
  actions: write

concurrency:
  group: careers-source-writers
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate Novva collector and source guard
        run: |
          node --check scripts/collect-novva-careers.mjs
          node --check scripts/validate-novva.mjs
      - name: Collect official Novva careers roles
        run: node scripts/collect-novva-careers.mjs
      - name: Enforce mission-fit role families
        run: node scripts/filter-mission-fit.mjs
      - name: Normalize locations and regions
        run: node scripts/normalize-job-locations.mjs
      - name: Deduplicate normalized jobs
        run: node scripts/dedupe-normalized-jobs.mjs
      - name: Run live QA
        run: node scripts/qa-site.mjs
      - name: Refresh regional coverage
        run: node scripts/refresh-region-status.mjs
      - name: Validate Novva employer-direct snapshot
        run: node scripts/validate-novva.mjs
      - name: Track first-seen dates
        run: node scripts/stamp-job-history.mjs
      - name: Validate updated feed
        run: |
          node scripts/validate.mjs
          node scripts/validate-job-history.mjs
          node scripts/validate-homepage-contract.mjs
          node scripts/validate-source-workflow-isolation.mjs
      - name: Commit verified Novva roles
        id: commit
        run: |
          if [ -z "$(git status --porcelain -- data/jobs.json data/novva-jobs.json data/job-history.json data/collector-status.json data/career-events.json data/qa-report.json)" ]; then
            echo "No verified Novva Data Centers data changes."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/jobs.json data/novva-jobs.json data/job-history.json data/collector-status.json data/career-events.json data/qa-report.json
          git commit -m "Refresh verified Novva Data Centers roles"
          git fetch origin main
          if ! git rebase origin/main; then
            git rebase --abort || true
            echo "::error::Could not rebase Novva refresh onto latest main."
            exit 1
          fi
          git push origin HEAD:main
          echo "changed=true" >> "$GITHUB_OUTPUT"
      - name: Deploy committed Novva data
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run pages.yml --repo "$GITHUB_REPOSITORY" --ref main -f deploy_only=true
