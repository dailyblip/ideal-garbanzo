name: Oracle stale fallback watchdog

on:
  schedule:
    - cron: '56 */6 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/enforce-oracle-fallback-freshness.mjs'
      - 'scripts/preserve-source-group.mjs'
      - 'scripts/validate-oracle-snapshot.mjs'
      - '.github/workflows/oracle-stale-fallback-watch.yml'

permissions:
  actions: write
  contents: write

concurrency:
  group: oracle-stale-fallback-watch
  cancel-in-progress: false

jobs:
  enforce:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Test Oracle fallback age policy and source boundary
        run: |
          node scripts/enforce-oracle-fallback-freshness.mjs --test
          node scripts/preserve-source-group.mjs --test
      - name: Capture pre-enforcement public-feed state
        run: cp data/jobs.json /tmp/oracle-freshness-baseline.json
      - name: Enforce Oracle fallback freshness
        run: node scripts/enforce-oracle-fallback-freshness.mjs
      - name: Verify Oracle fallback enforcement stayed source-scoped
        run: node scripts/preserve-source-group.mjs Oracle /tmp/oracle-freshness-baseline.json --verify
      - name: Validate Oracle snapshot and shared publication state
        run: |
          node scripts/validate.mjs
          node scripts/validate-job-history.mjs --allow-aging
          node scripts/validate-oracle-snapshot.mjs
          node scripts/validate-source-workflow-isolation.mjs
      - name: Publish Oracle freshness state from current main
        id: commit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          rebuild_from_latest_main() {
            git fetch origin main
            git reset --hard origin/main
            cp data/jobs.json /tmp/oracle-freshness-baseline.json
            node scripts/enforce-oracle-fallback-freshness.mjs --test
            node scripts/preserve-source-group.mjs --test
            node scripts/enforce-oracle-fallback-freshness.mjs
            node scripts/preserve-source-group.mjs Oracle /tmp/oracle-freshness-baseline.json --verify
            node scripts/validate.mjs
            node scripts/validate-job-history.mjs --allow-aging
            node scripts/validate-oracle-snapshot.mjs
            node scripts/validate-source-workflow-isolation.mjs
          }

          git fetch origin main
          if [ "$(git rev-parse HEAD)" != "$(git rev-parse origin/main)" ]; then
            echo "Main moved during Oracle freshness enforcement; rebuilding from latest main."
            rebuild_from_latest_main
          fi

          for attempt in 1 2 3; do
            git add -u -- data/jobs.json data/oracle-jobs.json data/collector-status.json
            if git diff --cached --quiet; then
              echo "Oracle fallback state is unchanged."
              echo "changed=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi

            git commit -m "Enforce Oracle fallback freshness"
            if git push origin HEAD:main; then
              echo "changed=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi

            echo "Main moved during Oracle freshness publish attempt ${attempt}; rebuilding before retry."
            sleep $((attempt * 2))
            rebuild_from_latest_main
          done

          echo "::error::Could not publish Oracle freshness state after three attempts."
          exit 1
      - name: Deploy Oracle freshness change
        if: steps.commit.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run pages.yml --repo "$GITHUB_REPOSITORY" --ref main -f deploy_only=true
