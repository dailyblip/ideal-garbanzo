name: Stream Data Centers source integrity guard

on:
  push:
    branches: [main]
    paths:
      - 'data/stream-data-centers-jobs.json'
      - 'data/jobs.json'
      - 'data/collector-status.json'
      - 'scripts/collect-stream-data-centers.mjs'
      - 'scripts/validate-stream-data-centers.mjs'
      - 'scripts/filter-mission-fit.mjs'
      - 'scripts/normalize-job-locations.mjs'
      - 'scripts/dedupe-normalized-jobs.mjs'
      - 'scripts/normalize-display-copy.mjs'
      - '.github/workflows/stream-data-centers-bootstrap.yml'
      - '.github/workflows/pages.yml'
      - '.github/workflows/stream-source-integrity-guard.yml'
  pull_request:
    branches: [main]
    paths:
      - 'data/stream-data-centers-jobs.json'
      - 'data/jobs.json'
      - 'data/collector-status.json'
      - 'scripts/collect-stream-data-centers.mjs'
      - 'scripts/validate-stream-data-centers.mjs'
      - 'scripts/filter-mission-fit.mjs'
      - 'scripts/normalize-job-locations.mjs'
      - 'scripts/dedupe-normalized-jobs.mjs'
      - 'scripts/normalize-display-copy.mjs'
      - '.github/workflows/stream-data-centers-bootstrap.yml'
      - '.github/workflows/pages.yml'
      - '.github/workflows/stream-source-integrity-guard.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check Stream collector and integrity validator
        run: |
          node --check scripts/collect-stream-data-centers.mjs
          node --check scripts/validate-stream-data-centers.mjs
      - name: Run Stream fail-closed regression tests
        run: |
          node scripts/collect-stream-data-centers.mjs --test-classifier
          node scripts/validate-stream-data-centers.mjs --test
      - name: Re-run publication transforms
        run: |
          node scripts/filter-mission-fit.mjs
          node scripts/normalize-job-locations.mjs
          node scripts/dedupe-normalized-jobs.mjs
          node scripts/normalize-display-copy.mjs
      - name: Validate exact Stream authoritative-public parity
        run: node scripts/validate-stream-data-centers.mjs
